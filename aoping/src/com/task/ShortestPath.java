package com.task;

import java.util.List;

// TASK: find shortest path from A to B with:
// Directed, Acyclic Graph. Edges.weight=1 (e.g. constant).
// if there is an edge from Y to X => Y is in X's neighbours.
// input is A, B and root nodes (nodes without edges to them)
// return space delimited nodes from A to B, or empty String if no such path.
public class ShortestPath {

	String findShortestPath(int idA, int idB, List<Node> roots) {
		
		
		
		
		return "";
	}
	
	static class Node{
		int id;
		List<Node> neighbours;
	}
}
